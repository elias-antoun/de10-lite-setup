{
  "name": "de10-lite-visit-logs",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "dev": "wrangler dev",
    "check": "wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "engines": { "node": ">=22.22.3" },
  "devDependencies": {
    "wrangler": "4.136.3",
    "miniflare": "5.20260921.0-alpha"
  }
}
